import { Theme } from "@mui/material/styles";
import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme: Theme) => ({
  root: {},
  high_index156_background: {
    backgroundColor: "#8f1f5c",
  },
}));

export default useStyles;
