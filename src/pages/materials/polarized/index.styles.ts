import { Theme } from "@mui/material/styles";
import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme: Theme) => ({
  root: {},
  trivex_parasol_background: {
    backgroundColor: "#474c38",
  },
}));

export default useStyles;
