import { Theme } from "@mui/material/styles";
import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme: Theme) => ({
  root: {},
  cr39_background: {
    backgroundColor: "#0391cd",
  },
}));

export default useStyles;
