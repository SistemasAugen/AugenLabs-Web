import { Theme } from "@mui/material/styles";
import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme: Theme) => ({
  root: {},
  trivex_background: {
    backgroundColor: "#04908D",
  },
}));

export default useStyles;
