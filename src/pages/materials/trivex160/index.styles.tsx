import { Theme } from "@mui/material/styles";
import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme: Theme) => ({
  root: {},
  trivex160_background: {
    backgroundColor: "#349353",
  },
}));

export default useStyles;
